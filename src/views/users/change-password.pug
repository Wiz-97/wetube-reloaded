extends ../base

block content
    if errorMessage
        span=errorMessage
    form(method="POST")
        input(placeholder="현재 비밀번호", name="oldPassword", type="password")
        input(placeholder="새로운 비밀번호", name="newPassword", type="password")
        input(placeholder="새로운 비밀번호 확인", name="newPasswordConfrim", type="password")
        input(value="Change Password", type="submit")